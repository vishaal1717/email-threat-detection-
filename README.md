# email-threat-detection-
I'm building a FastAPI backend for an email threat detection add-on. Create a backend/app/main.py with a /health endpoint, CORS enabled for all origins (for development), and a POST /api/analyze/raw endpoint that accepts JSON {"headers": str, "body": str} and for now just returns the headers split into a dictionary. Explain each part simply.
